package profile

import (
	"fmt"
)

func DeactivateCmd() {
	fmt.Println("Windows does not support terminal session exiting via an application")
	fmt.Println("We are currently looking for alternative solutions. \n")
	fmt.Println("Please use the 'exit' command to deactivate the local environment.")
}
